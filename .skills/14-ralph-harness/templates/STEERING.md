# Steering (human override)

Edit this file while the build loop runs to inject critical, do-it-first work.
The build agent checks this every iteration before picking a task. Remove items
when done.

(Currently empty — no steering.)

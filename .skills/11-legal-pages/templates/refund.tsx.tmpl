{/* IMPORTANT: have counsel review before launch. Generated by skill 11-legal-pages; not legal advice. */}
import { createFileRoute } from '@tanstack/react-router'

export const Route = createFileRoute('/_marketing/refund')({
  head: () => ({ meta: [{ title: 'Refund Policy · {{BRAND}}' }] }),
  component: RefundPage,
})

function RefundPage() {
  return (
    <main className="mx-auto w-full max-w-3xl px-4 py-12 sm:px-6">
      <article className="prose prose-zinc max-w-none dark:prose-invert">
        <p className="text-sm text-zinc-500 dark:text-zinc-400">Last updated: {{EFFECTIVE_DATE}}</p>
        <h1>Refund Policy</h1>
        <p>{{REFUND_SUMMARY}}</p>

        <h2>Pricing</h2>
        <p>{{PRICING_CLAUSE}}</p>

        <h2>Questions</h2>
        <p>
          Anything unclear? Email <a href="mailto:{{SUPPORT_EMAIL}}">{{SUPPORT_EMAIL}}</a> or use our{' '}
          <a href="/contact">contact form</a> and a human will answer.
        </p>
      </article>
    </main>
  )
}
